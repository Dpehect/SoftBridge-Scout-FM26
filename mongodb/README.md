# MongoDB Atlas
Create a free Atlas cluster, create a database user, allow Render outbound access (`0.0.0.0/0`), then set `MongoDb__ConnectionString` and `MongoDb__DatabaseName=fm26scout` in Render. Collections and indexes are created automatically at API startup.
