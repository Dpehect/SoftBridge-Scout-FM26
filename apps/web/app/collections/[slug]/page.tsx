import {getCollection,money} from "../../../lib/api";import Link from "next/link";
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const x=await getCollection(slug);return <main className="page container"><header className="page-head"><span className="eyebrow">COLLECTION</span><h1>{x.collection.name}</h1><p>{x.collection.description}</p></header><div className="recommendation-list">{x.players.map((p:any,i:number)=><Link href={`/players/${p.slug}`} className="panel recommendation" key={p.id}><span className="rank">{String(i+1).padStart(2,"0")}</span><div><h2>{p.fullName}</h2><p>{p.position} · {p.club??"Free agent"} · {p.country}</p></div><div className="score"><strong>{p.potentialAbility}</strong><span>POTENTIAL</span></div><div><b>{money(p.marketValue)}</b></div></Link>)}</div></main>}
