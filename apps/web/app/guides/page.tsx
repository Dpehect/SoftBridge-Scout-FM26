import Link from "next/link";import {getArticles} from "../../lib/api";export const metadata={title:"FM26 Guides"};
export default async function Page(){const x=await getArticles();return <main className="page container"><header className="page-head"><span className="eyebrow">KNOWLEDGE BASE</span><h1>FM26 guides</h1><p>Practical scouting, transfers and squad-building analysis.</p></header><div className="card-grid">{x.map((a:any)=><Link className="panel feature-card" href={`/guides/${a.slug}`} key={a.slug}><span className="chip">{a.category}</span><h2>{a.title}</h2><p>{a.summary}</p><b>Read guide →</b></Link>)}</div></main>}
