import { getPlayers } from "@/lib/api"; import { PlayerCard } from "@/components/player-card";
export default async function Players({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const p=await searchParams;const data=await getPlayers(p);return <main className="container" style={{padding:"48px 0"}}><div style={{display:"flex",justifyContent:"space-between",gap:20,alignItems:"end",marginBottom:24}}><div><div className="muted">OYUNCU VERİTABANI</div><h1 style={{fontSize:48,margin:"6px 0",letterSpacing:"-.05em"}}>Doğru profili bul</h1><p className="muted">{data.totalCount} oyuncu arasından filtrele.</p></div></div><form className="panel" style={{padding:16,display:"grid",gridTemplateColumns:"2fr repeat(4,1fr) auto",gap:10,marginBottom:20}}><input name="search" defaultValue={p.search} placeholder="Oyuncu ara"/><select name="position" defaultValue={p.position??""}><option value="">Tüm mevkiler</option>{["GK","CB","RB","DM","CM","AMR","AML","ST"].map(x=><option key={x}>{x}</option>)}</select><input name="maxAge" defaultValue={p.maxAge} placeholder="Maks. yaş" type="number"/><input name="minPotential" defaultValue={p.minPotential} placeholder="Min. potansiyel" type="number"/><select name="sort" defaultValue={p.sort??"potential-desc"}><option value="potential-desc">Potansiyel</option><option value="ability-desc">Mevcut seviye</option><option value="value-asc">En ucuz</option><option value="age-asc">En genç</option></select><button className="btn btn-primary">Filtrele</button></form><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(250px,1fr))",gap:14}}>{data.items.map(x=><PlayerCard key={x.id} player={x}/>)}</div></main>}
