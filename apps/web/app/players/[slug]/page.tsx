import { getPlayer,money } from "@/lib/api"; import { notFound } from "next/navigation";
export default async function PlayerPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;let p:any;try{p=await getPlayer(slug)}catch{return notFound()}return <main className="container" style={{padding:"48px 0"}}><section className="panel" style={{padding:28,display:"grid",gridTemplateColumns:"1.6fr 1fr",gap:28}}><div><div className="chip">{p.countryCode} · {p.position}</div><h1 style={{fontSize:58,letterSpacing:"-.06em",margin:"18px 0 8px"}}>{p.fullName}</h1><p className="muted" style={{fontSize:18}}>{p.club??"Serbest oyuncu"} · {p.age} yaş · {p.heightCm} cm · {p.personality}</p><div style={{display:"grid",gridTemplateColumns:"repeat(4,1fr)",gap:10,marginTop:28}}>{[["Mevcut",p.currentAbility],["Potansiyel",p.potentialAbility],["Değer",money(p.marketValue)],["Haftalık",money(p.weeklyWage)]].map(([l,v])=><div key={l as string} style={{background:"#09150f",padding:14,borderRadius:14,border:"1px solid var(--line)"}}><div className="muted" style={{fontSize:12}}>{l}</div><strong style={{fontSize:22}}>{v}</strong></div>)}</div></div><div><div className="muted" style={{fontSize:12,marginBottom:10}}>EN UYGUN ROLLER</div>{p.roleScores.slice(0,5).map((r:any)=><div key={r.slug} style={{marginBottom:12}}><div style={{display:"flex",justifyContent:"space-between",fontSize:13}}><span>{r.role}</span><strong>{r.score}</strong></div><div style={{height:6,background:"#08130e",borderRadius:99,overflow:"hidden",marginTop:6}}><div style={{width:`${r.score}%`,height:"100%",background:"var(--accent)"}}/></div></div>)}</div></section><section style={{display:"grid",gridTemplateColumns:"repeat(3,1fr)",gap:14,marginTop:14}}>{p.attributeGroups.map((g:any)=><div className="panel" style={{padding:20}} key={g.name}><h2 style={{marginTop:0}}>{g.name}</h2>{Object.entries(g.values).map(([k,v]:any)=><div key={k} style={{display:"flex",justifyContent:"space-between",padding:"9px 0",borderBottom:"1px solid var(--line)"}}><span className="muted">{k}</span><strong style={{color:v>=16?"var(--accent)":"var(--text)"}}>{v}</strong></div>)}</div>)}</section></main>}
