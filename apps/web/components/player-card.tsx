import Link from "next/link"; import { Player,money } from "@/lib/api"; import { ArrowUpRight, Sparkles } from "lucide-react";
export function PlayerCard({player}:{player:Player}){return <Link href={`/players/${player.slug}`} className="panel" style={{padding:18,display:"grid",gap:16,transition:"transform .2s",minHeight:220}}><div style={{display:"flex",justifyContent:"space-between",alignItems:"start"}}><div className="chip">{player.countryCode} · {player.position}</div>{player.isWonderkid&&<span className="chip" style={{color:"var(--accent2)",borderColor:"#2a6a4d"}}><Sparkles size={13}/> Wonderkid</span>}</div><div><h3 style={{fontSize:22,margin:"0 0 6px",letterSpacing:"-.03em"}}>{player.fullName}</h3><div className="muted">{player.club??"Serbest oyuncu"} · {player.age} yaş</div></div><div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:10}}><Metric label="Mevcut" value={player.currentAbility}/><Metric label="Potansiyel" value={player.potentialAbility}/></div><div style={{display:"flex",justifyContent:"space-between",alignItems:"end"}}><div><div className="muted" style={{fontSize:12}}>Piyasa değeri</div><strong>{money(player.marketValue)}</strong></div><ArrowUpRight color="var(--accent)"/></div></Link>}
function Metric({label,value}:{label:string;value:number}){return <div style={{background:"#09150f",padding:10,borderRadius:12,border:"1px solid var(--line)"}}><div className="muted" style={{fontSize:11}}>{label}</div><strong style={{fontSize:20}}>{value}</strong></div>}
